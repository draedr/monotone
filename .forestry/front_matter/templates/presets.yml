---
label: Presets
hide_body: true
fields:
- name: contacts
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: contacts
- name: links
  type: field_group_list
  fields:
  - name: image
    type: text
    config:
      required: false
    label: image
  - name: description
    type: text
    config:
      required: false
    label: description
    default: ''
  - name: name
    type: text
    config:
      required: false
    label: name
  - name: color
    type: text
    config:
      required: false
    label: color
  config:
    min: 
    max: 
    labelField: id
  label: links
pages:
- config/presets.json
