---
label: Presets
hide_body: true
fields:
- name: contacts
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: contacts
- name: links
  type: blocks
  label: links
  template_types:
  - presets-links
  config:
    min: 
    max: 
pages:
- config/presets.json
