---
label: Presets
hide_body: false
fields:
- name: contacts
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: contacts
- name: links
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: links
pages:
- config/presets.json
