---
label: Presets
hide_body: true
fields:
- name: contacts
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: contacts
- name: links
  type: include
  config: {}
  template: presets-links
  label: links
pages:
- config/presets.json
