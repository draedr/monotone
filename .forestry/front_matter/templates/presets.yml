---
label: Presets
hide_body: false
fields:
- name: contacts
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: contacts
- name: links
  type: blocks
  label: links
  template_types: []
  config:
    min: 
    max: 
pages:
- config/presets.json
